% Definir la función f(x)
function f = fun_exp(x)
  f = (exp(x) + exp(-x)) / 2;
end

% Definir la variable simbólica x
syms x;

% Calcular la serie de McLaurin hasta el término n
n = 5; % Número de términos
serie_taylor = taylor(fun_exp(x),x,n);

% Mostrar la serie de McLaurin
disp(serie_taylor);

% Valor de x para evaluar la serie
valor_x = 1;

% Sustituir x en la serie de McLaurin y evaluar
resultado = subs(serie_taylor, x, valor_x);

% Mostrar el resultado
disp(['Valor de la serie de McLaurin para x = ', num2str(valor_x), ': ', num2str(resultado)]);



syms x; % Definimos x como una variable simbólica

% Definimos la función f(x)
f = (exp(x) + exp(-x)) / 2;

% Definimos el punto alrededor del cual queremos desarrollar el polinomio de Taylor (en este caso, es x0 = 0)
x0 = 0;

% Orden del desarrollo de Taylor
n = 4; % Puedes ajustar este valor según el grado del polinomio deseado

% Calculamos la expansión de Taylor de f(x) alrededor de x0 hasta el orden n
taylor_expansion = taylor(f, x, 'Order', n);

% Mostramos la expansión de Taylor (también es la expansión de McLaurin en este caso)
disp(['La expansión de McLaurin de f(x) alrededor de x0 = ', num2str(x0), ' hasta el orden ', num2str(n), ' es:']);
disp(taylor_expansion);